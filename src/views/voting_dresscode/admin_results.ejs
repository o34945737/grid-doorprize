<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Admin Voting Dresscode - Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.datatables.net/2.1.8/css/dataTables.dataTables.min.css" rel="stylesheet">
</head>
<body class="p-4 bg-light">

<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <div>
      <h4 class="m-0">🏆 Voting Dresscode - Results</h4>
      <div class="text-muted small">Login: <b><%= admin.username %></b></div>
    </div>
    <div class="d-flex gap-2">
      <a href="/voting-dresscode/admin" class="btn btn-outline-secondary btn-sm">Dashboard</a>
      <a href="/voting-dresscode/admin/candidates" class="btn btn-warning btn-sm">Manage Candidates</a>
      <a href="/voting-dresscode/admin/results.csv" class="btn btn-success btn-sm">Export CSV</a>
      <a href="/voting-dresscode/" class="btn btn-outline-secondary btn-sm">User Page</a>
      <a href="/voting-dresscode/admin/logout" class="btn btn-outline-danger btn-sm">Logout</a>
    </div>
  </div>

  <div class="row g-3 mb-3">
    <div class="col-md-3">
      <div class="card">
        <div class="card-body">
          <div class="text-muted">Candidates</div>
          <div class="fs-4 fw-bold"><%= totals.candidates %></div>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card">
        <div class="card-body">
          <div class="text-muted">Votes</div>
          <div class="fs-4 fw-bold"><%= totals.votes %></div>
        </div>
      </div>
    </div>
    <div class="col-md-6">
      <div class="card">
        <div class="card-body">
          <div class="fw-bold">Aturan Ranking</div>
          <div class="text-muted small">
            Urut berdasarkan <b>Vote Count</b> (terbanyak), lalu ID.
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-body table-responsive">
      <table id="tblResults" class="table align-middle">
        <thead>
          <tr>
            <th style="width:70px">Rank</th>
            <th style="width:80px">ID</th>
            <th style="width:120px">Photo</th>
            <th>Nama Photo</th>
            <th style="width:140px">🗳 Votes</th>
          </tr>
        </thead>
        <tbody>
          <% rows.forEach((r, idx) => { %>
            <tr>
              <td class="fw-bold">
                <% if (idx === 0) { %>🥇<% } else if (idx === 1) { %>🥈<% } else if (idx === 2) { %>🥉<% } %>
                #<%= idx + 1 %>
              </td>
              <td><%= r.id %></td>
              <td>
                <img src="<%= r.photo_url %>" width="90" height="90" style="border-radius:12px; object-fit:cover;">
              </td>
              <td>
                <div class="fw-bold"><%= r.photo_name %></div>
              </td>
              <td><b><%= r.vote_count %></b></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js"></script>
<script src="https://cdn.datatables.net/2.1.8/js/dataTables.min.js"></script>
<script>
  $(function(){
    new DataTable("#tblResults", { order: [[0, "asc"]], pageLength: 25 });
  });
</script>

</body>
</html>
