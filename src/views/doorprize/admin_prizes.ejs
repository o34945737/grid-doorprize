<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin - Prizes</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="container py-4">
  <div class="d-flex justify-content-between align-items-center">
    <h2>Kelola Hadiah (Import Excel)</h2>
    <div class="d-flex gap-2">
      <a href="/doorprize/doorprize/admin" class="btn btn-outline-secondary btn-sm">Dashboard</a>
      <a href="/doorprize/doorprize/admin/logout" class="btn btn-outline-secondary btn-sm">Logout</a>
    </div>
  </div>

  <% if (totals.isEnough) { %>
    <div class="alert alert-success mt-3">
      Hadiah cukup. Peserta tersisa: <b><%= totals.participantsLeft %></b> — Hadiah tersisa: <b><%= totals.prizeLeft %></b>
    </div>
  <% } else { %>
    <div class="alert alert-warning mt-3">
      Hadiah kurang untuk memastikan semua peserta dapat hadiah.
      Peserta tersisa: <b><%= totals.participantsLeft %></b> — Hadiah tersisa: <b><%= totals.prizeLeft %></b>
    </div>
  <% } %>

  <div class="card p-3 mt-3">
    <div class="fw-bold">Format Excel (.xlsx)</div>
    <div class="text-muted small">Kolom wajib: <code>prize_name</code> dan <code>qty</code>. Contoh: Laptop 5, Monitor 3, Mouse 2</div>

    <form class="mt-3" method="POST" action="/doorprize/admin/prizes/import" enctype="multipart/form-data">
      <div class="mb-2">
        <input type="file" name="file" accept=".xlsx" class="form-control" required>
      </div>
      <button class="btn btn-primary">Import (Replace)</button>
      <div class="text-muted small mt-2">
        * Replace mode: akan mengganti semua hadiah (hanya bisa jika belum ada pemenang).
      </div>
    </form>
  </div>

  <div class="mt-4">
    <h5>Daftar Jenis Hadiah</h5>
    <table class="table table-sm table-striped">
      <thead>
        <tr>
          <th>Hadiah</th>
          <th>Qty (Total)</th>
          <th>Unit Tersisa</th>
        </tr>
      </thead>
      <tbody>
        <% types.forEach(t => { %>
          <tr>
            <td><%= t.prize_name %></td>
            <td><%= t.qty %></td>
            <td><%= t.left_items %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
