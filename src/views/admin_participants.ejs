<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Admin - Participants</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body class="container py-4">
  <div class="d-flex justify-content-between align-items-center">
    <h2>Peserta</h2>
    <div class="d-flex gap-2">
      <a href="/admin" class="btn btn-outline-secondary btn-sm">Dashboard</a>
      <a href="/admin/logout" class="btn btn-outline-secondary btn-sm">Logout</a>
    </div>
  </div>

  <div class="card p-3 mt-3">
    <div class="fw-bold">Import Peserta (Excel .xlsx)</div>
    <div class="text-muted small">
      Kolom disarankan: <code>name</code> (wajib), <code>department</code> (opsional).
    </div>

    <form class="mt-3" method="POST" action="/admin/participants/import" enctype="multipart/form-data">
      <div class="mb-2">
        <input type="file" name="file" accept=".xlsx" class="form-control" required>
      </div>
      <button class="btn btn-primary">Import Peserta</button>
    </form>
  </div>

  <div class="mt-4">
    <table class="table table-sm table-striped">
      <thead>
        <tr>
          <th>ID</th>
          <th>Nama</th>
          <th>department</th>
          <th>Status</th>
          <th>Waktu Daftar</th>
        </tr>
      </thead>
      <tbody>
        <% participants.forEach(p => { %>
          <tr>
            <td><%= p.id %></td>
            <td><%= p.name %></td>
            <td><%= p.department || "-" %></td>
            <td><%= p.has_won ? "Sudah dapat hadiah" : "Belum" %></td>
            <td><%= p.created_at %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
